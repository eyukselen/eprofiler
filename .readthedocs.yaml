version: 2

build:
  os: ubuntu-22.04  # Use a stable Ubuntu LTS for reliable builds
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .  # This installs eprofiler so Sphinx can read your docstrings
